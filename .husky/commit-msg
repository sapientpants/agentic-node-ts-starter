#!/usr/bin/env sh

# Conventional commit check (lightweight)
msg_file="$1"
if ! grep -Eq "^(feat|fix|chore|docs|refactor|test|perf|build|ci)(\(.+\))?!?: .+" "$msg_file"; then
  echo "✖ Commit message must follow Conventional Commits, e.g., 'feat(api): add endpoint'"
  exit 1
fi
